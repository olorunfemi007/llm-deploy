apiVersion: v1
kind: Service
metadata:
  name: llama-server
  namespace: llm-inference
  labels:
    app.kubernetes.io/name: llama-server
    app.kubernetes.io/part-of: llm-stack
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: llama-server
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
